apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base

patches:
  - path: deployment.yml
  - patch: |-
      - op: replace
        path: /spec/template/spec/containers/1/image
        value: redis:6.2.1
    target:
      kind: Deployment
      name: deployment