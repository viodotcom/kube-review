apiVersion: batch/v1
kind: Job
metadata:
  generateName: job-
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kube-review
    spec:
      restartPolicy: Never
      containers:
      - name: job
        image: "PLACEHOLDER"
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: config
          mountPath: /config
        envFrom:
          - secretRef:
              name: job-secret
        resources:
          limits:
            cpu: "1"
            memory: 600Mi
          requests:
            cpu: 100m
            memory: 600Mi
      volumes:
      - name: config
        configMap:
          name: job-configmap
